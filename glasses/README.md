Built output of `talkpilot-app/glasses/even-hud` (the Even G2 HUD film and
the connectable console). Do not edit here — rebuild from the app repo with
`VITE_BASE=/talkpilot-releases/glasses/ npm run build` and copy `dist/` over.
